# Набор тестов

## База

1. Тест на соответствие формата -log

2. Тест на работу флага -C

3. Передать в -C несуществующий абсолютный путь

4. Передать в -C относительный путь

5. Передать в -C несуществующий относительный путь

6. Тест на включение ТОЛЬКО нужных файлов в линковку

7. Тест на обработку ассемблерных файлов (перемешка ассемблера и C++)

8. Тест на включение ТОЛЬКО нужных ассемблерных файлов в линковку

9. Тест на перемешку C и C++

10. Сброрка C

11. Тест на перемшку C и ассемблера

12. Тест на сборку ТОЛЬКО ассемблера

13. Тест на то, что измененные файлы будут пересобираться

14. Тест на то, что пересобираться будут файлы, зависящие от измененного файла (например если поменяли какой нибудь хедер, но может быть и сурс файл)

15. Проверить как сборка проходит, если в каком нибудь файле будет включен сурс файл (типа #include "../source.cpp"). Пересобираться должны ТОЛЬКО НУЖНЫЕ ФАЙЛЫ, другие не трогать

16. Сборка библиотек. Если у нас имя выходного файла подходит под формат статической или динамической библиотеки, то мы собираем соответствующую библиотеку

## Относительные пути в include

1. Обработка относительных путей в include типа folder/aboba.h

2. Обработка относительных путей в include типа ./folder/aboba.h

3. Обработка относительных путей в include типа folder1/folder2/aboba.h

4. Обработка относительных путей в include типа /folder/aboba.cpp

5. Обработка относительных путей в include типа ../../../file.h

6. Обработка относительных путей в include типа ../../folder1/folder2/aboba.h

7. Обработка относительных путей в include типа ././../../folder1/./../folder2/../folder1/aboba.h

8. Обработка относительных путей в include типа folder/aboba.h, но этот путь не существует

9. Обработка относительных путей в include типа ../../../file.h, но этот путь идет из пустой папки, где нет файлов

10. Обработка относительных путей в include типа folder/aboba.h, но по этому пути есть несколько совпадений

11. Обработка относительных путей в include типа ../../folder1/folder2/aboba.h, но по этому пути есть несколько совпадений

12. Обработка относительных путей в include типа ././../../folder1/./../folder2/../folder1/aboba.h, путь действительно существует, но он вне карты который построил belder

13. Обработка абсолютных путей в include типа /home/folder/aboba.cpp

## Проверка базовых опций

1. проверка run опции

2. проверка config опции

3. проверка --rebuild и -reb 

4. --relink, -rel

5. -o [filename]

6. -o -flag (с ошибкой)

7. -o [конец строки] (с ошибкой)

8. -o [относительный путь к файлу]

## Проверки по --no-link-force

1. --no-link-force [file] [file] [file] (простые имена)

2. --no-link-force Mylib (где то лежат обе библиотеки)

3. --no-link-force Mylib (где то лежит одна библиотека .a)

4. --no-link-force Mylib (где то лежит одна библиотека .so)

5. --no-link-force Mylib.a

6. --no-link-force Mylib.so

7. --no-link-force [file] [file] [file] (относительные пути разного вида)

8. --no-link-force [file] [file] [file] (несуществующие файлы)

9. --no-link-force [file] [file] [file] (относительные пути, по которым подходят несколько файлов)

10. --no-link-force [file] [file] [file] (несуществующие относительные пути к существующим файлам)

11. --no-link-force [file] [file] [file] (относительные пути к несуществующим файлам)

12. --no-link-force -flag (неправильный набор)

13. --no-link-force [конец строки]

14. --no-link-force [file] [file] (файлы которые уже есть в списке no include)

15. --no-link-force [file] [file] (файлы которые уже есть в списке force include)

## Проверки по --link-force

1. --link-force [file] [file] [file] (простые имена)

2. --link-force Mylib (где то лежат обе библиотеки)

3. --link-force Mylib (где то лежит одна библиотека .a)

4. --link-force Mylib (где то лежит одна библиотека .so)

5. --link-force Mylib.a

6. --link-force Mylib.so

7. --link-force [file] [file] [file] (относительные пути разного вида)

8. --link-force [file] [file] [file] (несуществующие файлы)

9. --link-force [file] [file] [file] (относительные пути, по которым подходят несколько файлов)

10. --link-force [file] [file] [file] (несуществующие относительные пути к существующим файлам)

11. --link-force [file] [file] [file] (относительные пути к несуществующим файлам)

12. --link-force -flag (неправильный набор)

13. --link-force [конец строки]

14. --link-force [file] [file] (файлы которые уже есть в списке no include)

15. --link-force [file] [file] (файлы которые уже есть в списке force include)

## Проверки по --default-link

1. --default-link [file] [file] [file] (простые имена)

2. --default-link Mylib (где то лежат обе библиотеки)

3. --default-link Mylib (где то лежит одна библиотека .a)

4. --default-link Mylib (где то лежит одна библиотека .so)

5. --default-link Mylib.a

6. --default-link Mylib.so

7. --default-link [file] [file] [file] (относительные пути разного вида)

8. --default-link [file] [file] [file] (несуществующие файлы)

9. --default-link [file] [file] [file] (относительные пути, по которым подходят несколько файлов)

10. --default-link [file] [file] [file] (несуществующие относительные пути к существующим файлам)

11. --default-link [file] [file] [file] (относительные пути к несуществующим файлам)

12. --default-link -flag (неправильный набор)

13. --default-link [конец строки]

14. --default-link [file] [file] (файлы которые уже есть в списке no include)

15. --default-link [file] [file] (файлы которые уже есть в списке force include)

## -I family

1. -I[абсолютный путь к папке]

2. -I[относительный путь к папке]

3. -I[сложный относительный путь к папке]

4. -I[абсолютный путь к несуществующей папке]

5. -I[относительный путь к несуществующей папке]

6. -I[путь к папке, которая есть в no include списке папок]

7. --no-include [абсолютный путь к папке]

8. --no-include [относительный путь к папке]

9. --no-include [относительный путь к несуществующей папке]

10. --no-include [абсолютный путь к несуществующей папке]

11. --no-include [путь к папке, которая уже указана в -I списке]

12. --no-include [путь к папке 1] [путь к папке 2]

13. --defult-include [абсолютный путь к папке]

14. --defult-include [относительный путь к папке]

15. --defult-include [относительный путь к несуществующей папке]

16. --defult-include [абсолютный путь к несуществующей папке]

17. --defult-include [путь к папке, которая уже указана в -I списке]

18. --defult-include [путь к папке, которая уже указана в no include списке]

19. --defult-include [путь к папке 1] [путь к папке 2]

## Other Flags

1. Проверить линковку библиотеки по -l[short lib name]

2. Проверить линковку библиотеки по --link-force [short lib name]

3. Проверить работу флагов --clear-options, --clean-options 

4. Проверить работу флагов смены компилятора --CXX [c++ compiler] --CC [c compiler]

5. Проверить работу флагов смены стандарта C++ и C --C++standart [c++ standart] --Cstandart [c standart]

6. Проверить работу флагов, очищающих флаги из конфига --clear-flags, --clean-flags, --flags-clean, --flags-clear

## Compile/link flags

1. --compile-flags [many flags and options], проверить что эти флаги будут использованы только при компиляции, но не при линковке

2. --link-flags [many flags and options], проверить что эти флаги будут использованы только при линковке, но не при компиляции


## Other tests

1. Выдавать конфликты, если не может решить какой cpp файл подключить (одинаковые функции)

2. Выдавать конфликты, если есть два .h файла подходящие под include (потому что непонятно, куда дальше рекурсией искать инклюды)

3. Установка и работа pocketbuilder

4. Проверка на нахождения начального файла, проверка на то, что он только один. Если не можем найти начальный файл - ошибка, если несколько файлов подходят под указанный относительный путь или под указанное имя - выдавать соответствующую ошибку

5. Проверить что файлы с одним именем но по разному пути обрабатываются нормально. То есть например если у нас в проекте есть несколько файлов aboba.cpp, но они лежат в разных папках, то проект все еще должен нормально собраться без ошибок и проблем 

6. Проверка что если какие то файлы удаляются, то удаляются файлы зависимостей и соответствующие sym файлы

7. Проверить что сами папки в builder сами удяляются как только он не находит папку с сурс кодом при каждом запуске белдера. То есть если у нас был проект на компьютере, а потом его удалили, то belder должен это понять и удалить у себя соответствующую папку

8. Проверить что если компиляция хоть одного файла прошла плохо, то заканчиваем процесс. Если компиляция хотя бы одного файла вышла с ошибкой, то belder не должен ничего линковать

9. Проверить, что все нормально собирается в новый файл при смене начального файла. То есть если начальный файл меняется, то belder также должен выдать результат и выдать новый файл

---------------------------------------------

ПРОМПТ

Следующий проект лежит в репозитории по ссылке [ссылка], я хочу чтобы ты создал там новую ветку и в этой новой ветке сделал пул реквест, если ты не можешь создать новую ветку, то скажи мне и я создам ее сам. Проект - автоматическая система сборки для C, C++, Asm. ОЧЕНЬ ВНИМАТЕЛЬНО ПОСМОТРИ КОД ПРОЕКТА, ВАЖНО ЧТОБЫ ТЫ ПОНИМАЛ ЧТО ПРОИСХОДИТ ВНУТРИ. Задача - создать гугл тесты для этог проекта. Перед тем как интегрировать GTests сначала надо поразмышлятьк как это делать, потому что я использую make как систему сборки. Желательно в Makefile добавить опцию make test. Я набросал список тестов, которые должна пройти программа, их список ты можешь видеть выше. Я сам не уверен, что это эффективный вариант, ты можешь предложить какую нибудь другую систему, которая тестирует отдельные функции, но главное чтобы belder соответствовал всем требованиям, на каждую опцию и флаг выдавал правильный результат и работал правильно. Ты можешь придумать какие нибудь unit тесты, но лично я тут не вижу как можно это устроить. Подумай что еще нужно протестировать и что я упустил в списке, вполне возможно надо протестировать не только правильную работу, но и множество сценариев, где он должен выдавать какие то ошибки, такие сценарии я мог упустить. Проверь, чтобы все тесты полностью покрывали функционал и у пользователя не могло возникнуть неверной работы программы. Возможно в начало тестов нужно добавить предупреждение, что в процессе тестов будут использованы другие компиляторы и если таких на компьютере нет, то соответствующие тесты упадут.
Если у тебя есть хоть малейшие вопросы по тестам то задай их перед реализацией, очень важно чтобы ты понял на 100 процентов что нужно писать.




Множество конфигураций в одной папке, разделяются по входному файлу? Это надо в createEssentials и main идти

Сделать финальный тест-босс с какой нибудь библиотекой ардуино (несколько варинатов файла которые надо будте собрать)

Переписать string? Переписать vector?